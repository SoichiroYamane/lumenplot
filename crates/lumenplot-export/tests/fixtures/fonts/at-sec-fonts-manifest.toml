# AT-SEC-FONTS fixture manifest (M5-P3i slice).
#
# This file is the license/provenance evidence for the ONE vendored
# deterministic test font. It satisfies the manifest row for LP-SEC-007
# (font license manifest) and LP-TEXT-007 (reproducibility manifest:
# font bytes identity, face, variation, fallback route, license evidence)
# under ADR-0007 without claiming searchable-PDF, shaping-stack, or
# subsetter evidence, which remain staged for later P3 slices.
#
# The font bytes are test-only fixtures. They are not a dependency pin,
# not a public API, and not a distribution license grant beyond the
# OFL-1.1 terms recorded here. O-17 supply-chain review of the staged
# shaping/subset stack happens at the pin slice (ii), not here.

schema = 1
font_file = "FiraSans-Regular.ttf"
sha256 = "c29556a2719bf613ef3d5e070e40d903a8965d9c081beca1375dc1e6e0f93c23"
bytes = 456996
face_index = 0
family = "Fira Sans"
full_name = "Fira Sans Regular"
postscript_name = "FiraSans-Regular"
variation = "static-regular; no fvar table"
fstype = 0
fstype_meaning = "installable embedding"
license = "OFL-1.1"
license_file = "OFL.txt"
copyright = "Copyright (c) 2012-2015, The Mozilla Foundation and Telefonica S.A."
fallback_route = "PrimaryFont fixture only; no system fallback is used for export"
source_url = "https://github.com/google/fonts (ofl/firasans/FiraSans-Regular.ttf and ofl/firasans/OFL.txt)"
