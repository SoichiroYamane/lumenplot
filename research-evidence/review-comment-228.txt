comment 228 | task t_a8b289f7 | author luna-worker
================================================================================
Independent review result: CHANGES_REQUIRED for HEAD 7032d84e527364a484424a9cfcb4c5a2a46be569 (tree 3e301b18cd8055cca9ce80c5a865adcdb2d2b845), branch wt/t_14894021; worktree was clean and ancestry/scope matched the requested public base and implementation parent.

Blocking findings:
1. `crates/lumenplot-python/src/lib.rs:124-180` checks NumPy's `is_aligned()` then calls `readonly.as_array()` without validating the logical byte span/capacity or the zero-length data pointer. Reproduction against the built extension accepted `np.lib.stride_tricks.as_strided(np.array([0.0]), shape=(2,), strides=(8,))` with logical nbytes 16 and base nbytes 8, violating API-0003's malformed-byte-span rejection and allowing an out-of-bounds read. A zero-length `np.frombuffer(bytearray(16), dtype=np.float64, count=0, offset=1)` reports NumPy `aligned=True` but reaches ndarray's alignment panic; the call surfaced `pyo3_runtime.PanicException` and the panic hook printed an absolute cargo-registry source path, violating the aligned-input and redacted-panic contract. Add explicit stride/capacity/pointer-alignment validation before `as_array`, keep safe positive/negative/zero strides, and ensure input traversal panics become sanitized `LumenPlotError` (with regression fixtures).
2. `.github/workflows/phase3a2-wheel.yml:39-46` assumes Rust/Cargo and `cargo deny` exist inside the pinned manylinux image. The host `dtolnay/rust-toolchain` action cannot provision binaries inside the container. Live Quay manifest/config matched the pinned image/config digests, while the exact manylinux source build (commit 44507355cea84456f157a1d183575d43cc81e890, image created 2026-08-15) has no Rust/cargo installation and no `requirements-tools/cargo-deny`; the workflow therefore fails before wheel build. Use a builder image/tool provisioning plan that actually supplies the pinned Rust and cargo-deny offline, and record the provenance.
3. The same image's exact `requirements-tools/auditwheel` source pins `auditwheel==6.7.0`, but ADR/checker/manifest require and claim 6.8.0 (`scripts/phase3a2-manifest.py:110-112`). The workflow neither installs nor verifies auditwheel's version, so generated evidence would misreport the builder. Select a matching image or hash-pin/provision the required tool and update the accepted input record.
4. The build/runtime container mounts `--tmpfs /tmp:rw,noexec` while using `/tmp/cargo-target`, `/tmp/build-site`, and `/tmp/venv*` for Cargo build outputs, maturin, and native extension imports. Compiled build scripts/proc macros and the native `.so` need executable/loading storage; separate an executable build/venv mount or adjust the mount policy without weakening the intended read-only/non-root boundary.
5. Version is duplicated: `pyproject.toml:7` and `scripts/phase3a2-sbom.py:33` hardcode 0.1.0 even though the contract says Cargo-derived/single-source. Maturin 1.14.1's `merge_version` takes `project.version` when present, so a Cargo version change can silently produce a wheel/SBOM mismatch. Make the metadata/SBOM derive from Cargo or fail closed on the duplicate.

Evidence/gates: `cargo fmt --all -- --check`, locked workspace check/test/clippy, Nix dev-shell repeat, `nix flake check --all-systems --no-build --no-update-lock-file`, static architecture checker, and 162 checker mutation tests passed. Direct built-extension Python fixtures passed 6/6 and additional normal dtype/stride/error/determinism checks passed, but malformed-span and zero-length alignment probes exposed findings above. `--phase3a2-evidence` correctly failed closed because no CI manifest exists. Docker is unavailable locally, and no hosted wheel manifest/logs were supplied; do not release downstream child t_fa07c6c0 or claim Phase-3A2 evidence until a corrected workflow produces and independently reviews the four-cell same-wheel manifest.
