comment 226 | task t_a8b289f7 | author luna-worker
================================================================================
Review of exact head `7032d84e527364a484424a9cfcb4c5a2a46be569` (parent `1c86df1632d0c53a58025804b6527f698bccf061`, base `43a76bb24c1e6374329d53844f30fbbfbc4e67a0`): CHANGES REQUIRED; no PASS.

Findings:

1. **P1 — malformed NumPy byte spans are dereferenced instead of rejected.** `crates/lumenplot-python/src/lib.rs:89-180` checks ndim/length/alignment/dtype, then iterates `readonly.as_array()` without proving that every logical element address lies inside the backing allocation. An exact built-in ndarray made with `np.lib.stride_tricks.as_strided(base, shape=(2,), strides=(1_000_000_000,))` is accepted and the local extension exits with `-11` (SIGSEGV). This violates the accepted malformed-byte-span/stride contract and is memory-unsafe. Validate stride arithmetic and the full min/max byte span before creating/iterating the view, and add a regression fixture that expects `LumenPlotError(code='invalid-input')`.

2. **P1 — the direct container does not establish the Rust/tool/audit input inventory it claims.** `.github/workflows/phase3a2-wheel.yml:23-27` installs Rust on the host, while the `--network=none` build container only receives `/usr/local/cargo` and does not receive the host rustup/toolchain or install a reviewed Rust 1.89.0 toolchain. `RUSTUP_TOOLCHAIN=1.89.0` cannot make an absent toolchain appear. The workflow also invokes `cargo deny` (prefetch) and `abi3audit` (build) without installing or verifying them in the pinned image, and merely runs `rustc --version`, `cargo --version`, `auditwheel`, and `abi3audit` without asserting the versions recorded in the manifest. Fix the container tool inventory and capture/assert exact versions before relying on the manifest.

3. **P1 — SBOM/checksum evidence is incomplete but is reported as passed.** `scripts/phase3a2-sbom.py:15-29` looks for a nonexistent `checksum` field on Cargo metadata package records. Running it against this locked metadata produced 49 components with 0 hashes. `scripts/phase3a2-manifest.py:89-91` only checks `bomFormat`/`specVersion`; it accepts even a two-key incomplete SBOM and still emits `cargo_locked_sources_checksums_licenses: true`. It also hardcodes SBOM root version `0.1.0` at line 33. Build the SBOM from `Cargo.lock`/verified metadata with registry source, checksum, license, and version coverage; validate the complete component set and Cargo-derived version before setting the check true.

4. **P1 — ELF/RPATH validation is only logging.** `.github/workflows/phase3a2-wheel.yml:78-87` prints `DT_NEEDED|RPATH|RUNPATH` and rejects only `libpython|libcuda`; it never fails on any RPATH/RUNPATH or enforces an allowlist for DT_NEEDED. `auditwheel show` is not parsed, and `phase3a2-manifest.py` hardcodes `elf`/`elf_rpath` true. Enforce and record the actual architecture, symbol/version, dependency, and RPATH/RUNPATH policy.

5. **P1 — RECORD verification accepts omitted entries.** `scripts/phase3a2-manifest.py:34-50` validates only rows present in RECORD and does not require exactly one hashed row for every non-RECORD ZIP entry (nor reject duplicate ZIP names). Removing one hashed RECORD row from a local wheel still produced `wheel.record: true`. Compare the complete archive entry set with RECORD and reject omissions/duplicates before claiming integrity.

6. **P2 — Cargo is not the single package-version source.** `pyproject.toml:7` repeats `version = "0.1.0"`, and the SBOM generator repeats it independently. Current values happen to agree, but a Cargo version change can leave package/SBOM metadata stale; make the PEP-517 version dynamic/Cargo-derived and pass the derived value into SBOM generation with a checked equality.

7. **P2 — per-cell input-wheel rehashes occur after installation.** In `.github/workflows/phase3a2-wheel.yml`, each `pip install` precedes its `sha256sum --check`/`INPUT_WHEEL_SHA256` check (for example lines 99-104). The accepted same-wheel procedure requires rehashing the same input immediately before every install and recording that value; move the explicit check before each install (the pip hash requirement remains an additional check).

Verification performed: default static checker passed (`workspace architecture: OK`, `phase3a2 static contract: OK`); explicit evidence mode correctly failed closed because no CI-local manifest exists; 162 architecture mutation tests passed; `cargo fmt --all -- --check`, locked workspace check/test/clippy, `nix flake check --all-systems --no-build --no-update-lock-file`, `git diff --check`, and the six local Python helper tests passed. The local malformed-span regression reproduced SIGSEGV. Exact Docker same-wheel evidence, `cargo deny`, actionlint, and yamllint could not run here because Docker/cargo-deny/actionlint/yamllint are unavailable.
